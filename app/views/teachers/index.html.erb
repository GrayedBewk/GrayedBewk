<% @teachers.each do |t| %>
  <div id="teacher">
    <h1><%= t.name %></h1>
    <table>
      <thead>
        <tr>
         <th>Name</th>
         <th>E-mail</th>
         <th colspan=3>Options</th>
        </tr>
      </thead>
      <tbody>
        <% t.students.each do |s| %>
          <tr id="student">
            <td><%= s.name %></td>
            <td><%= s.email %></td>
            <td><%= link_to 'See grades', student_path(s.id) %></td>
                <% %>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
<% end %>

<<<<<<< HEAD
<h1>Listing Teachers</h1>


<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Password digest</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @teachers.each do |teacher| %>
      <tr>
        <td><%= teacher.name %></td>
        <td><%= teacher.email %></td>
        <td><%= teacher.password_digest %></td>
        <td><%= link_to 'Show', teacher %></td>
        <td><%= link_to 'Edit', edit_teacher_path(teacher) %></td>
        <td><%= link_to 'Destroy', teacher, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<ul>
<li><%= link_to 'Add teacher', new_teacher_path() %></li>
<li><%= link_to 'Add parent', new_parent_path() %></li>
<li><%= link_to 'Add student', new_student_path() %></li>
<li><%= link_to "Change your password", edit_teacher_path(session[:user_id]) %></li>
</ul>
